---
import SiteLayout from '../../../layouts/SiteLayout.astro';

const schema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@context": "https://schema.org",
      "@type": "WebApplication",
      "name": "URL Encoder Decoder — Encode & Decode URLs Online",
      "url": "https://siliconbased.dev/url-encoder-decoder",
      "description": "Free online URL encoder and decoder with live conversion, URL parser, query string builder, and percent encoding support. Runs entirely in your browser.",
      "applicationCategory": "DeveloperApplication",
      "operatingSystem": "Any",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD"
      },
      "author": {
        "@type": "Organization",
        "name": "GAB Ventures",
        "url": "https://gab.ae"
      }
    },
    {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "What is URL encoding (percent encoding)?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "URL encoding, also called percent encoding, replaces unsafe or reserved characters in a URL with a percent sign (%) followed by their two-digit hexadecimal value. For example, a space becomes %20 and an ampersand becomes %26. This is defined in RFC 3986 and ensures URLs are transmitted correctly across the internet."
          }
        },
        {
          "@type": "Question",
          "name": "What is the difference between encodeURI and encodeURIComponent?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "encodeURI encodes a full URL but preserves characters that have special meaning in URLs like ://?#[]@!$&'()*+,;=. encodeURIComponent encodes everything except letters, digits, and - _ . ~ making it suitable for encoding individual query parameter values or path segments where reserved characters must be escaped."
          }
        },
        {
          "@type": "Question",
          "name": "How do I URL encode a string?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Paste or type your text into the input panel with Encode mode selected. Choose 'Full URL' mode to preserve URL structure characters, or 'Component' mode to encode everything (ideal for query parameter values). The encoded output appears instantly on the right."
          }
        },
        {
          "@type": "Question",
          "name": "How do I decode a URL-encoded string?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Switch to Decode mode using the toggle at the top, then paste your percent-encoded string into the input panel. The decoded plain text appears on the right in real time. This works for both encodeURI and encodeURIComponent encoded strings."
          }
        },
        {
          "@type": "Question",
          "name": "What characters need to be URL encoded?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Characters that must be percent-encoded include spaces, non-ASCII characters (like accented letters or emoji), and reserved characters when used outside their special meaning: : / ? # [ ] @ ! $ & ' ( ) * + , ; =. Unreserved characters (A-Z, a-z, 0-9, - _ . ~) are never encoded."
          }
        },
        {
          "@type": "Question",
          "name": "Is my data safe when using this URL encoder?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Yes. All encoding and decoding happens entirely in your browser using JavaScript. No data is sent to any server. Your URLs and text never leave your device."
          }
        },
        {
          "@type": "Question",
          "name": "How does the URL parser work?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Paste any URL into the URL parser section and it will break it down into its components: protocol (http/https), host, port, path, query parameters (displayed as a key-value table), and fragment. This uses the browser's built-in URL API for accurate parsing."
          }
        }
      ]
    },
    {
      "@context": "https://schema.org",
      "@type": "Article",
      "headline": "URL Encoder Decoder — Encode & Decode URLs Online",
      "description": "Free online URL encoder and decoder with live conversion, URL parser, query string builder, and percent encoding support.",
      "datePublished": "2026-03-21T00:00:00Z",
      "publisher": {
        "@type": "Organization",
        "name": "siliconbased.dev",
        "url": "https://siliconbased.dev"
      }
    }
  ]
};

const htmlBody = `<div class="page-content">
<div class="hero">
  <h1>URL Encoder & Decoder</h1>
  <p>Free URL encoder and URL decoder with live percent encoding. Parse URLs into components, build query strings, and reference encoded characters — all in your browser.</p>
</div>

<div class="main">

  <!-- Options Bar -->
  <div class="options-bar">
    <div class="options-left">
      <label class="toggle-label">
        <input type="radio" name="encType" id="encFull" checked />
        <span class="toggle-text">Full URL (encodeURI)</span>
      </label>
      <label class="toggle-label">
        <input type="radio" name="encType" id="encComponent" />
        <span class="toggle-text">Component (encodeURIComponent)</span>
      </label>
    </div>
    <div class="options-right">
      <button class="mode-btn active" id="btnEncode">Encode</button>
      <button class="mode-btn" id="btnDecode">Decode</button>
    </div>
  </div>

  <!-- Two-panel layout -->
  <div class="tool-grid">
    <!-- Input -->
    <div class="pane">
      <div class="input-header">
        <div class="header-title" id="inputLabel">Text Input</div>
        <div class="header-actions">
          <button class="action-btn" id="clearBtn">Clear</button>
        </div>
      </div>
      <textarea id="inputArea" class="code-area" spellcheck="false" placeholder="Type or paste text / URL here..."></textarea>
      <div class="char-count" id="inputCount">0 characters</div>
    </div>

    <!-- Output -->
    <div class="pane">
      <div class="output-header">
        <div class="header-title" id="outputLabel">Encoded Output</div>
        <div class="header-actions">
          <button class="copy-btn" id="copyBtn">Copy</button>
        </div>
      </div>
      <textarea id="outputArea" class="code-area output" readonly spellcheck="false" placeholder="Output will appear here..."></textarea>
      <div class="char-count" id="outputCount">0 characters</div>
    </div>
  </div>

  <!-- URL Parser -->
  <div class="parser-section">
    <div class="input-header">
      <div class="header-title">URL Parser</div>
    </div>
    <div class="parser-input-row">
      <input type="text" id="parserInput" class="parser-input" placeholder="Paste a URL to parse (e.g. https://example.com/path?key=value&foo=bar#section)" spellcheck="false" />
      <button class="action-btn" id="parseBtn">Parse</button>
    </div>
    <div id="parserResult" class="parser-result" style="display:none;">
      <table class="parser-table" id="parserTable">
        <tbody id="parserBody"></tbody>
      </table>
      <div id="paramsSection" style="display:none;">
        <div class="params-title">Query Parameters</div>
        <table class="params-table">
          <thead><tr><th>Key</th><th>Value</th></tr></thead>
          <tbody id="paramsBody"></tbody>
        </table>
      </div>
    </div>
  </div>

  <!-- Query String Builder -->
  <div class="builder-section">
    <div class="input-header">
      <div class="header-title">Query String Builder</div>
      <div class="header-actions">
        <button class="action-btn" id="addParamBtn">+ Add Pair</button>
        <button class="copy-btn" id="copyQueryBtn">Copy</button>
      </div>
    </div>
    <div id="builderPairs" class="builder-pairs"></div>
    <div class="builder-output-row">
      <input type="text" id="builderOutput" class="parser-input builder-output" readonly placeholder="Query string will appear here..." />
    </div>
  </div>

  <!-- Common Characters Reference Table -->
  <div class="ref-section">
    <div class="input-header">
      <div class="header-title">Common Percent-Encoded Characters</div>
    </div>
    <div class="ref-table-wrap">
      <table class="ref-table">
        <thead><tr><th>Character</th><th>Encoded</th><th>Description</th><th>encodeURI</th><th>encodeURIComponent</th></tr></thead>
        <tbody>
          <tr><td>(space)</td><td>%20</td><td>Space character</td><td>%20</td><td>%20</td></tr>
          <tr><td>!</td><td>%21</td><td>Exclamation mark</td><td>! (kept)</td><td>%21</td></tr>
          <tr><td>#</td><td>%23</td><td>Hash / fragment</td><td># (kept)</td><td>%23</td></tr>
          <tr><td>$</td><td>%24</td><td>Dollar sign</td><td>$ (kept)</td><td>%24</td></tr>
          <tr><td>&amp;</td><td>%26</td><td>Ampersand / param separator</td><td>&amp; (kept)</td><td>%26</td></tr>
          <tr><td>'</td><td>%27</td><td>Apostrophe</td><td>' (kept)</td><td>%27</td></tr>
          <tr><td>(</td><td>%28</td><td>Open parenthesis</td><td>( (kept)</td><td>%28</td></tr>
          <tr><td>)</td><td>%29</td><td>Close parenthesis</td><td>) (kept)</td><td>%29</td></tr>
          <tr><td>+</td><td>%2B</td><td>Plus sign</td><td>+ (kept)</td><td>%2B</td></tr>
          <tr><td>,</td><td>%2C</td><td>Comma</td><td>, (kept)</td><td>%2C</td></tr>
          <tr><td>/</td><td>%2F</td><td>Forward slash / path separator</td><td>/ (kept)</td><td>%2F</td></tr>
          <tr><td>:</td><td>%3A</td><td>Colon</td><td>: (kept)</td><td>%3A</td></tr>
          <tr><td>;</td><td>%3B</td><td>Semicolon</td><td>; (kept)</td><td>%3B</td></tr>
          <tr><td>=</td><td>%3D</td><td>Equals / key-value separator</td><td>= (kept)</td><td>%3D</td></tr>
          <tr><td>?</td><td>%3F</td><td>Question mark / query start</td><td>? (kept)</td><td>%3F</td></tr>
          <tr><td>@</td><td>%40</td><td>At sign</td><td>@ (kept)</td><td>%40</td></tr>
          <tr><td>[</td><td>%5B</td><td>Open bracket</td><td>[ (kept)</td><td>%5B</td></tr>
          <tr><td>]</td><td>%5D</td><td>Close bracket</td><td>] (kept)</td><td>%5D</td></tr>
        </tbody>
      </table>
    </div>
  </div>

  <!-- SEO Content + FAQ -->
  <div class="seo-content">
    <h2>What is URL Encoding?</h2>
    <p>URL encoding, also known as percent encoding, is a mechanism defined in RFC 3986 for encoding information in a Uniform Resource Identifier (URI). It replaces unsafe or reserved characters with a percent sign (%) followed by their two-digit hexadecimal representation. For example, a space character becomes <code>%20</code>, an ampersand becomes <code>%26</code>, and a plus sign becomes <code>%2B</code>.</p>

    <h2>When to Use URL Encoding</h2>
    <ul>
      <li><strong>Query parameters:</strong> Encode values in URL query strings so that special characters like &amp;, =, and spaces don't break the URL structure.</li>
      <li><strong>Form submissions:</strong> HTML forms with <code>application/x-www-form-urlencoded</code> content type automatically percent-encode field values before sending.</li>
      <li><strong>API requests:</strong> Safely include user-generated content, file names, or search queries in API endpoint URLs.</li>
      <li><strong>Internationalized URLs:</strong> Encode non-ASCII characters (accented letters, CJK characters, emoji) so they can be used in URIs.</li>
      <li><strong>Redirect URLs:</strong> When passing a URL as a query parameter (e.g., a return URL), it must be encoded to avoid ambiguity.</li>
    </ul>

    <h2>encodeURI vs encodeURIComponent</h2>
    <table class="seo-table">
      <thead>
        <tr>
          <th>Feature</th>
          <th>encodeURI</th>
          <th>encodeURIComponent</th>
        </tr>
      </thead>
      <tbody>
        <tr><td>Purpose</td><td>Encode a complete URL</td><td>Encode a URL component (value)</td></tr>
        <tr><td>Preserves</td><td>: / ? # [ ] @ ! $ &amp; ' ( ) * + , ; =</td><td>Only A-Za-z0-9 - _ . ~</td></tr>
        <tr><td>Use case</td><td>Making a full URL safe</td><td>Encoding query param values</td></tr>
        <tr><td>Space</td><td>%20</td><td>%20</td></tr>
        <tr><td>&amp;</td><td>&amp; (preserved)</td><td>%26</td></tr>
        <tr><td>=</td><td>= (preserved)</td><td>%3D</td></tr>
      </tbody>
    </table>

    <h2>Frequently Asked Questions</h2>
    <div style="margin-top: 24px;">
      <h3 style="margin: 16px 0 8px; font-size: 1.1rem; color: #e0e0e0;">What is URL encoding (percent encoding)?</h3>
      <p style="margin-bottom: 16px;">URL encoding, also called percent encoding, replaces unsafe or reserved characters in a URL with a percent sign (%) followed by their two-digit hexadecimal value. For example, a space becomes %20 and an ampersand becomes %26. This is defined in RFC 3986 and ensures URLs are transmitted correctly across the internet.</p>

      <h3 style="margin: 16px 0 8px; font-size: 1.1rem; color: #e0e0e0;">What is the difference between encodeURI and encodeURIComponent?</h3>
      <p style="margin-bottom: 16px;">encodeURI encodes a full URL but preserves characters that have special meaning in URLs like ://?#[]@!$&amp;'()*+,;=. encodeURIComponent encodes everything except letters, digits, and - _ . ~ making it suitable for encoding individual query parameter values or path segments where reserved characters must be escaped.</p>

      <h3 style="margin: 16px 0 8px; font-size: 1.1rem; color: #e0e0e0;">How do I URL encode a string?</h3>
      <p style="margin-bottom: 16px;">Paste or type your text into the input panel with Encode mode selected. Choose 'Full URL' mode to preserve URL structure characters, or 'Component' mode to encode everything (ideal for query parameter values). The encoded output appears instantly on the right.</p>

      <h3 style="margin: 16px 0 8px; font-size: 1.1rem; color: #e0e0e0;">How do I decode a URL-encoded string?</h3>
      <p style="margin-bottom: 16px;">Switch to Decode mode using the toggle at the top, then paste your percent-encoded string into the input panel. The decoded plain text appears on the right in real time. This works for both encodeURI and encodeURIComponent encoded strings.</p>

      <h3 style="margin: 16px 0 8px; font-size: 1.1rem; color: #e0e0e0;">What characters need to be URL encoded?</h3>
      <p style="margin-bottom: 16px;">Characters that must be percent-encoded include spaces, non-ASCII characters (like accented letters or emoji), and reserved characters when used outside their special meaning: : / ? # [ ] @ ! $ &amp; ' ( ) * + , ; =. Unreserved characters (A-Z, a-z, 0-9, - _ . ~) are never encoded.</p>

      <h3 style="margin: 16px 0 8px; font-size: 1.1rem; color: #e0e0e0;">Is my data safe when using this URL encoder?</h3>
      <p style="margin-bottom: 16px;">Yes. All encoding and decoding happens entirely in your browser using JavaScript. No data is sent to any server. Your URLs and text never leave your device.</p>

      <h3 style="margin: 16px 0 8px; font-size: 1.1rem; color: #e0e0e0;">How does the URL parser work?</h3>
      <p style="margin-bottom: 16px;">Paste any URL into the URL parser section and it will break it down into its components: protocol (http/https), host, port, path, query parameters (displayed as a key-value table), and fragment. This uses the browser's built-in URL API for accurate parsing.</p>
    </div>

    <h2>Related Tools</h2>
    <ul>
      <li><a href="/base64-encoder-decoder">Base64 Encoder Decoder</a> — Encode and decode Base64 strings with file upload and URL-safe mode.</li>
      <li><a href="/jwt-decoder">JWT Decoder</a> — Decode and inspect JSON Web Tokens with header and payload analysis.</li>
      <li><a href="/regex-tester">Regex Tester</a> — Test and debug regular expressions online with real-time highlighting.</li>
    </ul>
  </div>

</div>
</div>`;

export const meta = {
  title: "URL Encoder Decoder — URL Encode & Decode Online",
  description: "Free online URL encoder and decoder with live percent encoding, URL parser, query string builder, and encodeURI vs encodeURIComponent modes. No data leaves your browser.",
  category: "tool",
  published: "2026-03-21",
};
---

<SiteLayout
  site="siliconbased"
  title="URL Encoder Decoder — URL Encode & Decode Online"
  description="Free online URL encoder and decoder with live percent encoding, URL parser, query string builder, and encodeURI vs encodeURIComponent modes. No data leaves your browser."
  canonical="https://siliconbased.dev/url-encoder-decoder"
  schema={schema}
>
  <Fragment set:html={htmlBody} />

  <script is:inline>
const inputArea = document.getElementById('inputArea');
const outputArea = document.getElementById('outputArea');
const inputLabel = document.getElementById('inputLabel');
const outputLabel = document.getElementById('outputLabel');
const inputCount = document.getElementById('inputCount');
const outputCount = document.getElementById('outputCount');
const encFull = document.getElementById('encFull');
const encComponent = document.getElementById('encComponent');
const btnEncode = document.getElementById('btnEncode');
const btnDecode = document.getElementById('btnDecode');
const clearBtn = document.getElementById('clearBtn');
const copyBtn = document.getElementById('copyBtn');
const parserInput = document.getElementById('parserInput');
const parseBtn = document.getElementById('parseBtn');
const parserResult = document.getElementById('parserResult');
const parserBody = document.getElementById('parserBody');
const paramsSection = document.getElementById('paramsSection');
const paramsBody = document.getElementById('paramsBody');
const addParamBtn = document.getElementById('addParamBtn');
const builderPairs = document.getElementById('builderPairs');
const builderOutput = document.getElementById('builderOutput');
const copyQueryBtn = document.getElementById('copyQueryBtn');

let mode = 'encode';

function init() {
  inputArea.addEventListener('input', convert);
  encFull.addEventListener('change', convert);
  encComponent.addEventListener('change', convert);

  btnEncode.addEventListener('click', () => setMode('encode'));
  btnDecode.addEventListener('click', () => setMode('decode'));

  clearBtn.addEventListener('click', () => {
    inputArea.value = '';
    outputArea.value = '';
    updateCounts();
    inputArea.focus();
  });

  copyBtn.addEventListener('click', () => {
    const text = outputArea.value;
    if (!text) return;
    navigator.clipboard.writeText(text).then(() => {
      copyBtn.textContent = '\u2713 Copied';
      setTimeout(() => { copyBtn.textContent = 'Copy'; }, 1500);
    });
  });

  // URL parser
  parseBtn.addEventListener('click', parseUrl);
  parserInput.addEventListener('keydown', (e) => {
    if (e.key === 'Enter') parseUrl();
  });

  // Query string builder
  addParamBtn.addEventListener('click', () => addParamRow('', ''));
  copyQueryBtn.addEventListener('click', () => {
    const text = builderOutput.value;
    if (!text) return;
    navigator.clipboard.writeText(text).then(() => {
      copyQueryBtn.textContent = '\u2713 Copied';
      setTimeout(() => { copyQueryBtn.textContent = 'Copy'; }, 1500);
    });
  });

  // Start with 2 empty rows
  addParamRow('', '');
  addParamRow('', '');

  updateCounts();
}

function setMode(m) {
  mode = m;
  btnEncode.classList.toggle('active', m === 'encode');
  btnDecode.classList.toggle('active', m === 'decode');
  inputLabel.textContent = m === 'encode' ? 'Text Input' : 'Encoded Input';
  outputLabel.textContent = m === 'encode' ? 'Encoded Output' : 'Decoded Output';
  inputArea.placeholder = m === 'encode' ? 'Type or paste text / URL here...' : 'Paste percent-encoded string here...';
  outputArea.placeholder = m === 'encode' ? 'Encoded output will appear here...' : 'Decoded text will appear here...';
  inputArea.value = '';
  outputArea.value = '';
  updateCounts();
}

function convert() {
  const input = inputArea.value;
  if (!input) {
    outputArea.value = '';
    updateCounts();
    return;
  }

  const useComponent = encComponent.checked;

  if (mode === 'encode') {
    try {
      outputArea.value = useComponent ? encodeURIComponent(input) : encodeURI(input);
    } catch (e) {
      outputArea.value = 'Error: ' + e.message;
    }
  } else {
    try {
      outputArea.value = useComponent ? decodeURIComponent(input) : decodeURI(input);
    } catch (e) {
      try {
        outputArea.value = decodeURIComponent(input);
      } catch (e2) {
        outputArea.value = 'Error: Invalid encoded string';
      }
    }
  }
  updateCounts();
}

function parseUrl() {
  const raw = parserInput.value.trim();
  if (!raw) return;

  try {
    const url = new URL(raw);
    parserBody.innerHTML = '';
    parserResult.style.display = 'block';

    const parts = [
      ['Protocol', url.protocol],
      ['Host', url.hostname],
      ['Port', url.port || '(default)'],
      ['Path', url.pathname],
      ['Search', url.search || '(none)'],
      ['Fragment', url.hash || '(none)']
    ];

    parts.forEach(function(p) {
      var tr = document.createElement('tr');
      tr.innerHTML = '<td class="parser-label">' + escapeHtml(p[0]) + '</td><td class="parser-value">' + escapeHtml(p[1]) + '</td>';
      parserBody.appendChild(tr);
    });

    // Query params
    var params = url.searchParams;
    var entries = [];
    params.forEach(function(value, key) { entries.push([key, value]); });

    if (entries.length > 0) {
      paramsSection.style.display = 'block';
      paramsBody.innerHTML = '';
      entries.forEach(function(entry) {
        var tr = document.createElement('tr');
        tr.innerHTML = '<td>' + escapeHtml(entry[0]) + '</td><td>' + escapeHtml(entry[1]) + '</td>';
        paramsBody.appendChild(tr);
      });
    } else {
      paramsSection.style.display = 'none';
    }
  } catch (e) {
    parserResult.style.display = 'block';
    parserBody.innerHTML = '<tr><td colspan="2" style="color:#f87171;">Invalid URL. Make sure it includes a protocol (e.g. https://)</td></tr>';
    paramsSection.style.display = 'none';
  }
}

function escapeHtml(str) {
  return str.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
}

// Query string builder
function addParamRow(key, value) {
  var row = document.createElement('div');
  row.className = 'builder-row';
  row.innerHTML = '<input type="text" class="builder-key" placeholder="Key" value="' + escapeHtml(key) + '" />'
    + '<span class="builder-eq">=</span>'
    + '<input type="text" class="builder-val" placeholder="Value" value="' + escapeHtml(value) + '" />'
    + '<button class="builder-remove" title="Remove">\u00d7</button>';

  row.querySelector('.builder-remove').addEventListener('click', function() {
    row.remove();
    buildQueryString();
  });
  row.querySelector('.builder-key').addEventListener('input', buildQueryString);
  row.querySelector('.builder-val').addEventListener('input', buildQueryString);

  builderPairs.appendChild(row);
  buildQueryString();
}

function buildQueryString() {
  var rows = builderPairs.querySelectorAll('.builder-row');
  var parts = [];
  rows.forEach(function(row) {
    var key = row.querySelector('.builder-key').value.trim();
    var val = row.querySelector('.builder-val').value;
    if (key) {
      parts.push(encodeURIComponent(key) + '=' + encodeURIComponent(val));
    }
  });
  builderOutput.value = parts.length > 0 ? '?' + parts.join('&') : '';
}

function updateCounts() {
  var ic = inputArea.value.length;
  var oc = outputArea.value.length;
  inputCount.textContent = ic.toLocaleString() + ' character' + (ic !== 1 ? 's' : '');
  outputCount.textContent = oc.toLocaleString() + ' character' + (oc !== 1 ? 's' : '');
}

init();
</script>

  <style is:inline>

.page-content { font-family: 'Inter', -apple-system, sans-serif; background: #060a12; color: #e0e0e0; min-height: 100vh; display: flex; flex-direction: column; }
code, pre, .mono { font-family: 'JetBrains Mono', monospace; }
.hero { text-align: center; padding: 48px 24px 24px; background: linear-gradient(180deg, #0f1729 0%, #060a12 100%); }
.hero h1 { font-size: 2.4rem; font-weight: 900; color: #fff; margin-bottom: 6px; }
.hero p { color: #888; font-size: 0.95rem; }
.main { max-width: 1100px; margin: 0 auto; padding: 24px 20px 80px; flex: 1; width: 100%; position: relative; }

/* Options Bar */
.options-bar { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 16px; margin-bottom: 20px; padding: 14px 18px; background: #0a1020; border: 2px solid #152040; border-radius: 12px; }
.options-left { display: flex; flex-wrap: wrap; gap: 18px; }
.options-right { display: flex; gap: 8px; }
.toggle-label { display: flex; align-items: center; gap: 8px; cursor: pointer; user-select: none; }
.toggle-label input[type="radio"] { width: 16px; height: 16px; accent-color: #818cf8; cursor: pointer; }
.toggle-text { font-size: 0.82rem; color: #aaa; font-weight: 500; }

/* Mode buttons */
.mode-btn { background: #1e2030; border: 1px solid #2a2d40; color: #888; padding: 6px 16px; border-radius: 8px; font-size: 0.8rem; font-weight: 600; cursor: pointer; font-family: 'Inter', sans-serif; transition: all 0.15s; }
.mode-btn:hover { background: #2a2d40; color: #ccc; }
.mode-btn.active { background: #818cf8; color: #fff; border-color: #818cf8; }

/* Tool grid */
.tool-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; margin-bottom: 32px; align-items: stretch; }
.pane { display: flex; flex-direction: column; }
.input-header, .output-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px; }
.header-title { font-size: 0.85rem; font-weight: 600; color: #ccc; text-transform: uppercase; letter-spacing: 0.5px; }
.header-actions { display: flex; gap: 8px; }
.action-btn { background: #1e2030; border: 1px solid #2a2d40; color: #888; padding: 6px 12px; border-radius: 8px; font-size: 0.75rem; font-weight: 500; cursor: pointer; font-family: 'Inter', sans-serif; transition: all 0.15s; }
.action-btn:hover { background: #2a2d40; color: #ccc; }
.copy-btn { background: #1e2030; border: 1px solid #2a2d40; color: #888; padding: 6px 14px; border-radius: 8px; font-size: 0.75rem; font-weight: 500; cursor: pointer; font-family: 'Inter', sans-serif; transition: all 0.15s; }
.copy-btn:hover { background: #2a2d40; color: #ccc; }

/* Character count */
.char-count { font-size: 0.72rem; color: #555; margin-top: 6px; text-align: right; font-family: 'JetBrains Mono', monospace; }

/* Code areas */
.code-area { width: 100%; min-height: 300px; background: #0a1020; border: 2px solid #152040; border-radius: 12px; padding: 16px; color: #e0e0e0; font-family: 'JetBrains Mono', monospace; font-size: 0.85rem; line-height: 1.6; resize: vertical; outline: none; transition: border-color 0.2s; white-space: pre-wrap; word-wrap: break-word; }
.code-area:focus { border-color: #818cf8; }
.code-area.output { background: #080e1a; color: #4ade80; cursor: default; }
.code-area::placeholder { color: #555; }

/* URL Parser */
.parser-section { margin-bottom: 32px; }
.parser-input-row { display: flex; gap: 10px; margin-bottom: 16px; }
.parser-input { flex: 1; background: #0a1020; border: 2px solid #152040; border-radius: 10px; padding: 12px 16px; color: #e0e0e0; font-family: 'JetBrains Mono', monospace; font-size: 0.85rem; outline: none; transition: border-color 0.2s; }
.parser-input:focus { border-color: #818cf8; }
.parser-result { background: #0a1020; border: 2px solid #152040; border-radius: 12px; padding: 16px; overflow-x: auto; }
.parser-table { width: 100%; border-collapse: collapse; }
.parser-table td { padding: 8px 14px; border-bottom: 1px solid #152040; font-size: 0.85rem; }
.parser-label { color: #888; font-weight: 600; width: 120px; text-transform: uppercase; font-size: 0.75rem; letter-spacing: 0.5px; }
.parser-value { color: #e0e0e0; font-family: 'JetBrains Mono', monospace; word-break: break-all; }
.params-title { font-size: 0.8rem; font-weight: 700; color: #ccc; text-transform: uppercase; letter-spacing: 0.5px; margin: 16px 0 10px; padding-top: 12px; border-top: 1px solid #152040; }
.params-table { width: 100%; border-collapse: collapse; }
.params-table th { text-align: left; padding: 8px 14px; border-bottom: 2px solid #2a2d4e; color: #888; font-weight: 600; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.5px; }
.params-table td { padding: 8px 14px; border-bottom: 1px solid #152040; color: #e0e0e0; font-family: 'JetBrains Mono', monospace; font-size: 0.82rem; word-break: break-all; }

/* Query String Builder */
.builder-section { margin-bottom: 40px; }
.builder-pairs { display: flex; flex-direction: column; gap: 8px; margin-bottom: 12px; }
.builder-row { display: flex; align-items: center; gap: 8px; }
.builder-key, .builder-val { flex: 1; background: #0a1020; border: 2px solid #152040; border-radius: 8px; padding: 10px 14px; color: #e0e0e0; font-family: 'JetBrains Mono', monospace; font-size: 0.82rem; outline: none; transition: border-color 0.2s; }
.builder-key:focus, .builder-val:focus { border-color: #818cf8; }
.builder-eq { color: #555; font-weight: 700; font-size: 1rem; }
.builder-remove { background: none; border: 1px solid #2a2d40; color: #555; width: 32px; height: 32px; border-radius: 8px; cursor: pointer; font-size: 1.1rem; display: flex; align-items: center; justify-content: center; transition: all 0.15s; }
.builder-remove:hover { background: #2a1020; border-color: #f87171; color: #f87171; }
.builder-output-row { display: flex; }
.builder-output { width: 100%; color: #4ade80; }

/* Reference section */
.ref-section { margin-bottom: 40px; }
.ref-table-wrap { overflow-x: auto; }
.ref-table { width: 100%; border-collapse: collapse; }
.ref-table th { text-align: left; padding: 10px 14px; border-bottom: 2px solid #2a2d4e; color: #ccc; font-weight: 600; font-size: 0.78rem; background: #0a1020; text-transform: uppercase; letter-spacing: 0.5px; white-space: nowrap; }
.ref-table td { padding: 10px 14px; border-bottom: 1px solid #152040; font-size: 0.85rem; color: #aaa; }
.ref-table td:nth-child(2) { color: #818cf8; font-family: 'JetBrains Mono', monospace; font-weight: 600; }
.ref-table td:nth-child(4), .ref-table td:nth-child(5) { font-family: 'JetBrains Mono', monospace; font-size: 0.8rem; color: #888; }
.ref-table tr:hover td { background: #0a1020; }

/* SEO Content */
.seo-content { margin-top: 48px; border-top: 1px solid #1e2030; padding-top: 48px; color: #b0b8d0; font-size: 0.95rem; line-height: 1.6; }
.seo-content h2 { color: #fff; font-size: 1.4rem; font-weight: 700; margin: 32px 0 16px; }
.seo-content h2:first-child { margin-top: 0; }
.seo-content h3 { color: #e0e0e0; font-size: 1.1rem; font-weight: 600; margin: 24px 0 12px; }
.seo-content p { margin-bottom: 16px; }
.seo-content ul { margin-bottom: 16px; padding-left: 24px; }
.seo-content li { margin-bottom: 8px; }
.seo-content a { color: #818cf8; text-decoration: none; }
.seo-content a:hover { text-decoration: underline; }
.seo-content code { background: #0a1020; padding: 2px 6px; border-radius: 4px; color: #818cf8; font-size: 0.85em; }
.seo-table { width: 100%; border-collapse: collapse; margin-bottom: 24px; font-size: 0.9rem; }
.seo-table th { text-align: left; padding: 10px 14px; border-bottom: 2px solid #2a2d4e; color: #ccc; font-weight: 600; background: #0a1020; }
.seo-table td { padding: 10px 14px; border-bottom: 1px solid #152040; color: #aaa; }
.seo-table tr:hover td { background: #0a1020; }

/* Responsive */
@media (max-width: 800px) {
  .tool-grid { grid-template-columns: 1fr; }
  .options-bar { flex-direction: column; align-items: flex-start; }
  .ref-grid { grid-template-columns: 1fr 1fr; }
  .code-area { min-height: 200px; }
  .parser-input-row { flex-direction: column; }
}
@media (max-width: 500px) {
  .ref-grid { grid-template-columns: 1fr; }
}

</style>
</SiteLayout>
